# 1. Auto-detect text files and normalize line endings (CRLF vs LF)
# This fixes the "Windows vs Mac/Linux" conflict automatically.
* text=auto

# 2. Treat these image files as binary (Do not try to fix line endings in images!)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary

# 3. Font files (Common in Flutter apps)
*.ttf binary
*.otf binary

# 4. Explicitly treat code files as text
*.dart text
*.py text
*.yaml text
*.html text
*.js text
*.css text
*.md text
*.json text

# 5. Windows scripts should always use Windows endings (CRLF)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 6. Linux/Mac scripts should always use Linux endings (LF)
*.sh text eol=lf